# Client build-time config (Vite). Copy to `.env` and set for your deployment.
#
# In development you don't need this — the client defaults to ws://localhost:2567.
# For a deployed client, point it at your deployed server over a SECURE socket:
VITE_SERVER_URL=wss://mini-ruderal.fly.dev
